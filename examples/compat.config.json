{
  "$schema": "../schemas/compat.config.v1.schema.json",
  "format": "compat-config/v1",
  "targets": {
    "chrome": "120"
  },
  "manual_requirements": [
    {
      "kind": "manual",
      "id": "behavior.structured-clone.transfer",
      "title": "Structured clone transfer semantics",
      "targets": {
        "chrome": "104"
      },
      "reason": "Application depends on a semantic boundary not represented by a single BCD key.",
      "source": [
        "wpt:html/webappapis/structured-clone/"
      ]
    }
  ]
}
